const initialState = {
  currentPage: "",
  lastPage: "",
};

const Reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default Reducer;
